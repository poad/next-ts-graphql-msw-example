"""
An RFC 3986, RFC 3987, and RFC 6570 (level 4) compliant URI string.
"""
scalar URI

type Pokemon {
  id: ID!
  name: String!
  visual: URI
}

# the schema allows the following query:
type Query {
  list: [Pokemon!]!
}
